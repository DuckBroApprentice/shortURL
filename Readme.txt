思路：

參考網路上的說法，需要準備：加密方法、map(redis)

加密方法採取比較多文章提到的base62
我的作法可能比較菜：
先隨機一個數(範圍在0~62^5)之間，可能的問題：也許會隨機到同一個key，所以產生後還要再排查一次，完成 if重覆 的邏輯
這個if邏輯在base62後處理，原因是存進redis的key是短網址

再把隨機到的數轉成62進制當作短網址

if 存在 && 值不相同  ==>  隨機到同一個數，但不同來源


想像：

使用者Post一串網址

網址轉換成一串加密短網址

而這個短網址要能觸發db

所以我在生成網址的時候要加上一些Get的想法
/localhost:8080/short?short=短網址  ==> maybe
*redirect redis get key ==> 長url

執行雖然沒有錯誤，但發現一個事前沒考慮週到的問題：同一次服務中，同一個網址每次執行都會產生不同的短網址  爛透了 XD
